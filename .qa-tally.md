# Adversarial QA Tally — Oversight Dispatch Manager

This file tracks what the QA loop has tested and found, so future runs don't
repeat the same paths. Append-only — update after every run.

## Conventions
- **Tested**: a path or surface that has been exercised
- **Found**: an issue (bug, confusing UX, broken state)
- **Fixed**: resolved in code, with commit SHA
- **Validated**: re-tested after fix

## Runs

### Run #1 — 2026-04-07

#### Tested
- Initial app load at http://localhost:5173
- Header "Dispatch" button (not yet clicked this run)
- Bottom dispatch tab handle visibility/badges
- Drawer open via tab handle
- Drawer open via header button
- Manager grouping detection (5 groups, 65 sessions)
- Drawer Esc-to-close
- Drawer backdrop click-to-close
- Keyboard shortcut "d" to toggle drawer
- Empty state of dispatch composer (no children selected)
- Selecting a single child + typing message + dispatching
- Selecting multiple children + broadcasting one message
- Closing drawer with X button
- Per-child status badges (active/idle/done) display correctly
- Cost aggregation totals per group
- DispatchDrawerHandle hides when drawer is open (verified earlier)
- Standalone bucket: solo sessions outside any group
- Drawer expansion of multiple manager groups simultaneously
- Selected count badge in drawer header

#### Found
(none yet recorded — this is the inaugural run)

### Run #2 — 2026-04-07 (QA loop)

#### Tested
- Initial load → inspected network + console + DOM state
- Raw SSE via curl (both direct to :3001 and via vite proxy :5173)
- Raw SSE via browser EventSource (probe)
- Verified /api/sessions returns 66, /api/managers returns 5 groups via XHR
- Keyboard "d" toggle WHILE textarea is focused (input guard test)
- Rapid 3× "d" press (race condition check)
- Dispatch composer disabled-button state (empty text, no selection)
- Drawer open → verify managers list populated after fix

#### Found
- **BUG #1 (critical, fixed)**: SSE stream `/api/stream` was buffered by the
  global `compression` middleware. With default `Accept-Encoding: gzip`,
  EventSource never fired `onopen`, the header was permanently stuck on
  "disconnected", and live updates never flowed. Repro: `curl -v /api/stream`
  returns 0 bytes until the stream fills a gzip block; `curl -H "Accept-Encoding: identity"`
  streams immediately. **Fix**: exempt `/api/stream` from compression in
  `server/middleware/performance.js`.
- **BUG #2 (minor, fixed)**: `useSSE` cleanup called `setConnected(false)`
  unconditionally, which under React StrictMode could race with the remounted
  EventSource's `onopen` and leave the header stuck on "disconnected" even
  when the new connection was healthy. **Fix**: drop the setState from
  cleanup in `client/src/hooks/useSSE.js`.
- **BUG #3 (minor, fixed)**: `npm run dev` in `server/` did not watch
  `./middleware` or `./lib`, so edits to rate limit, compression, CORS,
  config etc. silently required a manual restart. **Fix**: add both dirs
  to `--watch-path` in `server/package.json`.
- **Nit**: `GET /favicon.ico` → 404 on every load (logged as console error).
  Not fixed this run — needs a tiny favicon asset and/or route stub.

#### Validated
- Post-fix: `curl /api/stream` (default encoding) now streams headers +
  events immediately via both :3001 and :5173
- Post-fix: browser header badge `disconnected: false`
- Post-fix: opening drawer via "d" shows 5 groups • 66 sessions
- Post-fix: rapid 3× "d" keeps `disconnected: false` throughout
- Post-fix: typing "d" in drawer composer does NOT toggle drawer
  (input guard in useKeyboardShortcuts works correctly)
- Dispatch button correctly disabled when text empty or selection empty

#### Notes for future runs
- Skip: SSE "disconnected" baseline check — fixed and validated
- Skip: "d in composer" conflict — validated input guard blocks toggle
- Skip: rapid open/close race — no visible glitch
- Always check: console.errors after every navigation (still the favicon 404)
- Test next: favicon.ico — add tiny asset or 204 stub
- Test next: dispatch with extremely long message (overflow/scroll?)
- Test next: dispatch when network is broken (offline simulation)
- Test next: dispatch to a session that's currently busy (returns 409?)
- Test next: drawer behavior on small viewport (mobile width)
- Test next: collapsed Done column auto-expand when selecting from it
- Test next: optimistic message rendering edge cases (long text, code blocks)
- Test next: Resume ID copy-to-clipboard click on idle/done cards
- Test next: New session form validation with empty fields
- Test next: rate limiter behavior at the new 2000 cap
- Test next: SSE reconnect after server restart (does "disconnected" clear?)
- Test next: dispatch manager grouping when sessions move between groups
