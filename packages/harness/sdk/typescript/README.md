# TypeScript SDK Examples

Thin wrappers for Node/GitHub Actions CI. Invokes Python orchestrator — same JSON contract.

```bash
npx tsx sdk/typescript/examples/mission-loop.ts --dry-run
```

Requires Python orchestrator on PATH (`pip install -e sdk/python`).

See [docs/setup/cursor-sdk.md](../../docs/setup/cursor-sdk.md).
