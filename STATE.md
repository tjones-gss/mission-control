# STATE — Full Build self-assessment

_Generated by §1 self-assessment protocol. Loop date: 2026-06-24._

## Test baseline (this run)

| Suite | Result |
|---|---|
| Server (`vitest run`) | **1256 passed / 1 failed** — the single failure is `tests/contracts/cli-args.contract.test.js`, a **real-binary** test that spawns the actual `claude` CLI and times out at 5s. Environmental, not a code defect. |
| Client (`vitest run`) | **642 passed / 0 failed** (62 files) |
| Python | not run this loop (not part of §1 JS gates; pre-existing suite) |

> Note on tooling: `npm run test:cockpit` fails to resolve `vitest` via `npx` under the Bash tool on this box; tests run green via `./node_modules/.bin/vitest run` in each workspace. Commit-gate uses the direct binary.

## DoD criteria — green vs red (per §3 / DOD-LADDER.md)

Verified GREEN (artifact present + suite green):
- **L0-a / L0-b** — `parser_degraded` emitted across parsers (observed in test logs), `ParserDegradedBanner.jsx` present, parser tests pass.
- **L1-a** — `claude-cli.js buildSpawn()` (no `shell:true`); contract tests pass.
- **L1-b** — `lib/trust-store.js` per-cwd trust grant; tests pass.
- **L1-c** — fleet escalation deterministic (no claude in trust path); tests pass.
- **L1-d** — `packages/contracts/schema-version.json` single source; CI step "Schema-version cross-language parity"; `version-consistency.contract.test.js` + `schema-version.contract.test.js` pass.
- **L1-e** — `reconcileFleetRuns()` in `fleet-runner.js`; tests pass.
- **L1-f / L1-g** — fleet/loop suites green (server side); python `test_contract.py` present.
- **L2-a** — `WelcomeHero.jsx` present + tests.
- **L3-b** — `packages/contracts/SPEC.md` + `tools/generate-spec.mjs` + `spec-generator.contract.test.js`.
- **L3-c (partial)** — `lib/audit-log.js`, `lib/otel.js`, audit-wiring tests present.
- **V1** — MeshView complete (commit `8e4795c`, HEAD).

Not yet verified / likely incomplete (come AFTER V2 in execution order):
- **L2-b** — one-click rails adoption: no route/handler found. TODO.
- **L2-c** — CI coverage + e2e gates: `ci.yml` has parity; coverage/e2e gating unconfirmed.
- **L3-a** — cross-vendor label drop: docs check pending.
- **L3-c (rest)** — `/api/docs` OpenAPI **not mounted** (code comments mark it as later "S6" work).
- **L3-d** — release engineering (`release/` dir exists; tag/SBOM workflow unconfirmed).

## Phases complete (PROGRESS.md)

No prior `PROGRESS.md` / `BLOCKER.md`. Created `PROGRESS.md` this loop. V1 marked done.

## Phase to execute this run

**V2 — Pipeline Canvas.** Per §8 execution order `L0 → L1 → V2`; L0/L1 are green, V2 is entirely unbuilt (no `PipelineCanvas/`, no `routes/pipelines.js`). V2 does not depend on L2/L3. Build TDD-first.
